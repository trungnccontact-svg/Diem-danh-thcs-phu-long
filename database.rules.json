{
  "rules": {
    "users": {
      ".read": true,
      "$uid": {
        ".read": true,
        ".write": "auth != null && root.child('users').child(auth.uid).child('role').val() == 'admin'"
      }
    },
    "attendance": {
      ".read": "auth != null",
      "$date": {
        "$teacherUid": {
          ".read": "auth != null",
          ".write": "auth != null && (root.child('users').child(auth.uid).child('role').val() == 'admin' || auth.uid == $teacherUid)",
          ".validate": "newData.hasChildren(['teacherUid','teacherName','className','present','total','checkedAt']) && newData.child('teacherUid').val() == $teacherUid && newData.child('present').isNumber() && newData.child('total').isNumber() && newData.child('present').val() >= 0 && newData.child('present').val() <= newData.child('total').val()"
        }
      }
    },
    "fcmTokens": {
      ".read": "auth != null",
      ".write": true
    }
  }
}

